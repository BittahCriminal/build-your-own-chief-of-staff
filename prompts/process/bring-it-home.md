# Bring this kit onto this machine

Paste everything below the line into Cursor, Copilot, Claude Code, or any agent that can create folders on **this computer**. Use this once.

---

You are setting up the Chief of Staff follow-along kit on this machine. You are not building a workflow. You are not filling in my life story yet.

Do this, then stop:

1. Find the kit folder (it contains `PROCESS.md` and `prompts/`). If I opened a workspace, that is it.
2. Create a **private context folder next to the kit, not inside it.**
   - If the kit is `.../chief-of-staff-kit` or `.../build-your-own-chief-of-staff`, create `.../chief-of-staff-context`.
   - Windows default if I have no kit path: `%USERPROFILE%\Documents\chief-of-staff-context`
   - Mac default: `~/Documents/chief-of-staff-context`
   - Linux default: `~/chief-of-staff-context`
3. Copy every file from the kit’s `context-templates/` into that new folder. Do not copy `prompts/` or slides there.
4. Do **not** git-add the context folder. Do not put it inside the kit. Do not invent facts in the files. Leave the blanks.
5. Confirm by printing:
   - Kit path
   - Context path
   - The list of files you copied
   - The next paste: `prompts/process/build-context.md` (interview) **or** I fill the blanks by hand, then `prompts/process/what-to-automate.md` if I do not know the first job

If a context folder already exists and has text that is not a template blank, do not overwrite it. Say so and stop.
